# Allow guest user to connect remotely (needed for Aspire health checks in dev)
loopback_users = none

# Tell the management plugin to load the definitions file from the conf.d mount
management.load_definitions = /etc/rabbitmq/conf.d/definitions.json